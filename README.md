RoboticAutomationFactory is written in java. Main class is FactoryAutomationFunction.java.
- It has main method defined that takes height, width, length and mass of a package as input and calculates its volume. It compares its volume and mass with threashold limits and determines the stack of the package to deliver.
- To determine stack, constants are added as an enum.
